<nav style="background-color: black !important;" class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="/"><img src="/logo.png" height="40px" alt=""></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse"
            data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false"
            aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ml-auto mb-2 mb-lg-0">
                <% if (!user) { %>
                    <li class="nav-item">
                        <a <% if (title === 'Login') { %> 
                                class="nav-link active"
                            <% } else {%>
                                class="nav-link"
                            <%} %> aria-current="page" href="/login">
                            Login
                        </a>
                    </li>
                    <li class="nav-item">
                        <a <% if (title === 'Register') { %> 
                                class="nav-link active"
                            <% } else {%>
                                class="nav-link"
                            <%} %> aria-current="page" href="/register">
                            Register
                        </a>
                    </li>
                <% } else { %>
                    <li class="nav-item">
                        <a <% if (title === 'Home') { %> 
                                class="nav-link active"
                            <% } else {%>
                                class="nav-link"
                            <%} %> aria-current="page" href="/">
                            Home
                        </a>
                    </li>
                    <li class="nav-item">
                        <a <% if (title === 'Groups') { %> 
                                class="nav-link active"
                            <% } else {%>
                                class="nav-link"
                            <%} %> aria-current="page" href="/group">
                            Groups
                        </a>
                    </li>
                    <li class="nav-item">
                        <a <% if (title === 'Events') { %> 
                                class="nav-link active"
                            <% } else {%>
                                class="nav-link"
                            <%} %> aria-current="page" href="/event">
                            Events
                        </a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" id="profile-menu-dropdown" data-toggle="dropdown" href="/">
                            <img src="<%= user.profile_pic %>" alt="Profile" style="height:28px; width: 28px; border-radius: 100%;">
                        </a>
                        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="profile-menu">
                            <a href="/user/<%= user.id %> " class="dropdown-item">My Profile</a>
                            <form action="/logout" method="POST">
                                <input type="submit" value="Logout" class="dropdown-item">
                            </form>
                        </div>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>